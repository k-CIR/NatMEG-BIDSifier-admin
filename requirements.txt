# Core dependencies
numpy>=2.0.0
pandas>=2.0.0
pyyaml>=6.0
tqdm>=4.64.0

# MEG/EEG and BIDS conversion
mne>=1.5.0
mne-bids>=0.13.0

# Note: Dependencies like scipy, matplotlib, nibabel, h5py are installed
# automatically by mne and mne-bids as needed.

# Optional: Development and analysis tools
jupyter>=1.0.0
ipython>=8.0.0
bids-validator>=1.14.0
